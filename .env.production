# Production Environment Variables for Inventory System
# Generate a secure 32-character encryption key for PocketBase
# You can generate one using: openssl rand -hex 32
PB_ENCRYPTION_KEY=your-32-character-encryption-key-here

# URL of the PocketBase backend API
# For production with nginx proxy, use /api for relative requests
# For external access, use the server's IP or domain with port
VITE_API_URL=/api

# External port for PocketBase admin and API
# Change if port 8090 is already in use
POCKETBASE_PORT=8090

# External port for the frontend application
# Recommended: avoid port 80 on OMV (OMV admin UI usually uses it)
# Use 8080 for the inventory app, or change as needed
FRONTEND_PORT=8080